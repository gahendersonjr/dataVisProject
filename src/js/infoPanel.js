/** Class implementing the infoPanel view. */
class InfoPanel {

  constructor(worldData, country) {
	  this.worldData = worldData;
	  this.updateInfo(country);
	  
	  //setup table
  }

  updateInfo(country) {

	
  }

}
